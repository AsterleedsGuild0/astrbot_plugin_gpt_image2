[project]
name = "astrbot-plugin-gpt-image2"
version = "0.0.2"
description = "AstrBot GPT Image2 绘图插件"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["httpx>=0.27.0"]

[dependency-groups]
dev = [
    "pyyaml>=6.0.2",
]
