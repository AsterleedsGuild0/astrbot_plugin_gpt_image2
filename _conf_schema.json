{
  "api_key": {
    "description": "API Key",
    "type": "string",
    "hint": "第三方 OpenAI 兼容 API Key",
    "obvious_hint": true
  },
  "base_url": {
    "description": "API Base URL",
    "type": "string",
    "default": "https://api.openai.com/v1",
    "hint": "例如 https://api.example.com/v1，不要以 images/generations 结尾"
  },
  "api_mode": {
    "description": "API 模式",
    "type": "string",
    "default": "images",
    "options": ["images", "responses"],
    "labels": ["Images API", "Responses API"]
  },
  "model": {
    "description": "Images API 模型",
    "type": "string",
    "default": "gpt-image-2"
  },
  "responses_model": {
    "description": "Responses API 模型",
    "type": "string",
    "default": "gpt-5.5"
  },
  "images_prompt_rewrite_guard": {
    "description": "Images API 禁止重写提示词",
    "type": "bool",
    "default": false,
    "hint": "开启后会在 Images API 的 prompt 前加入 Prompt Guard：Use the following text as the complete prompt. Do not rewrite it:。默认关闭以保持原 Images API 行为。"
  },
  "responses_prompt_rewrite_guard": {
    "description": "Responses API 禁止重写提示词",
    "type": "bool",
    "default": true,
    "hint": "开启后会在 Responses API image_generation 输入前加入 Prompt Guard，减少模型改写提示词；关闭后更接近 ChatGPT Web 对复杂画面的自由理解和补全。"
  },
  "fallback_api_providers": {
    "description": "备用生图 API 站点",
    "type": "list",
    "default": [],
    "hint": "用于 draw/edit 一次性生图，以及 /plan confirm 最后的实际生图；Plan 对话整理阶段不使用该列表。WebUI 的列表项是字符串：点击 + 添加一条备用站点字符串。最简单写法只填 URL，例如 https://api-backup.example.com/v1，会继承主 api_key/api_mode/model/responses_model。若要指定独立 key、API 模式或模型，使用 key=value 写法：name=backup-1, base_url=https://api-backup.example.com/v1, api_key=sk-xxx, api_mode=responses, model=gpt-image-2, responses_model=gpt-5.5。官方兜底写法：role=authoritative_fallback, adaptive=false；该类站点固定在普通站点之后。调用顺序：普通站点按健康状态排序 → 权威兜底站点。",
    "items": {
      "description": "备用站点字符串",
      "type": "string",
      "default": "",
      "hint": "推荐写法 1：只填 URL，例如 https://api-backup.example.com/v1（继承主 API Key、API 模式和模型）。推荐写法 2：key=value，例如 name=backup-1, base_url=https://api-backup.example.com/v1, api_key=sk-xxx, api_mode=responses, model=gpt-image-2, responses_model=gpt-5.5。官方兜底追加 role=authoritative_fallback, adaptive=false。"
    }
  },
  "adaptive_provider_priority": {
    "description": "自适应调整生图站点优先级",
    "type": "bool",
    "default": true,
    "hint": "开启后插件会根据 draw/edit 和 /plan confirm 的生图成功/失败情况自动调整本次运行时的站点尝试顺序；不会改写 WebUI 配置。健康状态保存在 AstrBot data/plugin_data 下，测试包重装不会清除。"
  },
  "provider_failure_cooldown": {
    "description": "失败站点降级冷却时间（秒）",
    "type": "int",
    "default": 300,
    "hint": "站点失败后会在该时间内降低优先级，但不会被永久禁用；如果所有站点都在冷却中，仍会继续按健康评分尝试。"
  },
  "size": {
    "description": "图片尺寸",
    "type": "string",
    "default": "auto",
    "options": ["auto", "1024x1024", "1536x1024", "1024x1536"]
  },
  "quality": {
    "description": "图片质量",
    "type": "string",
    "default": "auto",
    "options": ["auto", "low", "medium", "high"]
  },
  "output_format": {
    "description": "输出格式",
    "type": "string",
    "default": "png",
    "options": ["png", "jpeg", "webp"]
  },
  "moderation": {
    "description": "内容审核强度",
    "type": "string",
    "default": "auto",
    "options": ["auto", "low"]
  },
  "output_compression": {
    "description": "输出压缩质量",
    "type": "int",
    "default": 0,
    "hint": "仅在输出格式不是 png 时生效；0 表示不发送该参数"
  },
  "n": {
    "description": "生成数量",
    "type": "int",
    "default": 1,
    "hint": "Responses API 模式下会并发多次请求"
  },
  "timeout": {
    "description": "请求超时时间（秒）",
    "type": "int",
    "default": 120
  },
  "response_format_b64_json": {
    "description": "请求返回 Base64 图片",
    "type": "bool",
    "default": true,
    "hint": "建议开启，可避免图片 URL 过期或访问失败"
  },
  "max_input_images": {
    "description": "最多输入参考图数量",
    "type": "int",
    "default": 4
  },
  "save_outputs": {
    "description": "保存生成结果",
    "type": "bool",
    "default": true
  },
  "send_copyable_prompt_after_success": {
    "description": "Plan 成功后发送可复制命令",
    "type": "bool",
    "default": true,
    "hint": "开启后 /plan confirm 成功生图会额外发送一条纯文本 /image2 draw 或 /image2 edit 命令，方便复制到其他地方复用；关闭可减少群聊刷屏。失败时的直接重试命令不受此项影响。"
  },
  "render_text_as_image": {
    "description": "文本回复转图片",
    "type": "bool",
    "default": true,
    "hint": "开启后插件文本回复使用 image2 自包含 Markdown 卡片模板，不加载外部 JS/CDN；失败则回退纯文本"
  },
  "text_image_width": {
    "description": "兜底文本图片宽度",
    "type": "int",
    "default": 1200,
    "hint": "仅用于插件内置兜底文转图，单位像素"
  },
  "text_image_font_size": {
    "description": "兜底文本图片字号",
    "type": "int",
    "default": 32,
    "hint": "仅用于插件内置兜底文转图"
  },
  "text_image_font_path": {
    "description": "兜底文本图片字体路径",
    "type": "string",
    "default": "",
    "hint": "仅用于插件内置兜底文转图；为空时自动选择系统中文字体"
  },
  "plan_enabled": {
    "description": "启用 Plan 模式",
    "type": "bool",
    "default": true
  },
  "plan_model": {
    "description": "Plan 模式 Responses 模型",
    "type": "string",
    "default": "gpt-5.4"
  },
  "plan_timeout": {
    "description": "Plan 会话超时时间（秒）",
    "type": "int",
    "default": 300,
    "hint": "用户空闲超时；模型思考和生图处理期间会自动延长"
  },
  "plan_max_rounds": {
    "description": "Plan 最大对话轮数",
    "type": "int",
    "default": 6
  },
  "plan_use_custom_api": {
    "description": "Plan 模式使用独立 API 配置",
    "type": "bool",
    "default": false,
    "hint": "开启后 plan_base_url 必须支持 /responses"
  },
  "plan_base_url": {
    "description": "Plan 独立 Responses API Base URL",
    "type": "string",
    "default": "",
    "hint": "仅在启用 plan_use_custom_api 时生效，不要以 /responses 结尾"
  },
  "plan_api_key": {
    "description": "Plan 独立 API Key",
    "type": "string",
    "default": "",
    "hint": "仅在启用 plan_use_custom_api 时生效"
  }
}
